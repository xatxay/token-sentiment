import styled from "@emotion/styled/macro";
import { menuColor } from "../color/color";

const TypewriteLoading = styled.div`
  color: ${menuColor};
  font-weight: bold;
`;

export { TypewriteLoading };
